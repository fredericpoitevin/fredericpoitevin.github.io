# Refreshing online
bundle exec jekyll build
git add .
git commit -m "init jekyll"
git push origin master

# Edit them
bundle show minima # show where minima's files are stored on computer
see https://github.com/jekyll/minima#

